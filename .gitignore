# Rust
/target
**/target

# Node
node_modules
packages/*/dist
package-lock.json
bun.lockb

# Agent tooling
.alfonso
.opencode
.cortexkit
_bmad-output
.bmad

# OS / editor
.DS_Store
*.swp

# Cross-repo doc-gen artifacts (generated, not source)
/ARCHITECTURE.md
/STRUCTURE.md

# Never commit real credential material, master keys, or vault databases
*.key
store.db
*.vault
/secrets
gate-evidence/

# The public-flip audit trail and any triaged-private docs. Nested git repo:
# versioned, never pushed to the public remote.
docs/private/
